# メール送信の2つの方法

## 📧 方法1: Stripeの自動メール（簡単・推奨）

### メリット
- ✅ **設定不要** - Stripeが自動で送ってくれる
- ✅ **SMTP設定不要** - 環境変数が不要
- ✅ **確実に届く** - Stripeが管理しているので安心

### デメリット
- ⚠️ メールの内容をカスタマイズできない（Stripeのデフォルトメール）
- ⚠️ ブランドロゴやデザインを変更できない

### 使い方
**何も設定しなくてOK！** Stripeが自動でメールを送ってくれます。

決済完了時に、Stripeが自動で：
- お客様に「決済が完了しました」メールを送信
- 領収書を送信

---

## 📧 方法2: 独自メール（カスタマイズ可能）

### メリット
- ✅ **完全にカスタマイズ可能** - メールの内容、デザインを自由に変更
- ✅ **ブランド統一** - サイトのデザインに合わせたメール

### デメリット
- ⚠️ **SMTP設定が必要** - GmailやSendGridなどの設定が必要
- ⚠️ **環境変数が増える** - 5〜6個の環境変数を設定する必要がある

### 使い方
現在の実装では、この方法が使えるようになっています。

必要な環境変数：
- `SMTP_HOST` - メールサーバーのアドレス
- `SMTP_PORT` - ポート番号（通常は587）
- `SMTP_USER` - メールアドレス
- `SMTP_PASS` - パスワード
- `SMTP_FROM` - 送信元メールアドレス（省略可）

---

## 🤔 どちらを選ぶべき？

### Stripeの自動メールを選ぶ場合
- ✅ 設定が簡単で済む
- ✅ メールの内容は標準的でOK
- ✅ すぐに使いたい

→ **何も設定しなくてOK！** Stripeが自動で送ってくれます。

### 独自メールを選ぶ場合
- ✅ メールの内容を完全にカスタマイズしたい
- ✅ ブランドロゴやデザインを統一したい
- ✅ 注文内容を詳しく記載したい

→ SMTP設定が必要です（`STRIPE_WEBHOOK_SETUP_SIMPLE.md` を参照）

---

## 💡 推奨

**初心者の方は「Stripeの自動メール」を推奨します。**

理由：
1. 設定が簡単（何もしなくてOK）
2. 確実にメールが届く
3. 後から独自メールに変更することも可能

Stripeの自動メールでも、以下の情報は含まれます：
- 決済が完了したことの通知
- 領収書
- 購入した商品の情報
- 金額

---

## 🔄 現在の設定

現在のコードでは、**両方の方法に対応**しています：

- **Stripeの自動メール**: 常に有効（設定不要）
- **独自メール**: SMTP環境変数を設定すると、追加で送信される

つまり、SMTPを設定しなくても、Stripeが自動でメールを送ってくれます！

SMTPを設定すると、**Stripeのメール + 独自メールの両方**が送られます。
