# Render プラン分析（jewelry-jay）

## 結論：**Free プランで運用可能**

このプロジェクトは Render の **Free（無料）プラン** で運用できます。  
「一番低いプラン」= **Free のままで問題ありません**。  
本格的に売上・アクセスが増えたら **Starter（$7/月）** へのアップグレードを検討すれば十分です。

---

## 1. アプリ構成の整理

| 項目 | 内容 | Free との相性 |
|------|------|----------------|
| **ランタイム** | Node.js（`node server.js`） | ✅ 対応 |
| **ビルド** | `buildCommand` 未指定 → `npm install` のみ | ✅ ビルド時間ほぼ不要 |
| **静的ファイル** | HTML / CSS / JS / products.json | ✅ 軽量 |
| **メディア（画像・動画）** | **すべて Cloudflare R2 の URL**（自サーバーから配信していない） | ✅ 帯域をほとんど消費しない |
| **API** | GET 商品一覧・詳細、POST Stripe チェックアウト | ✅ リクエスト数も軽い |
| **データ保存** | なし（DB なし、products.json はリポジトリから読み取りのみ） | ✅ エフェメラルディスクで問題なし |
| **外部呼び出し** | Stripe API（決済時のみ） | ✅ 過度な送信なし |

メディアを R2 に寄せているため、Render の「帯域・ディスク・CPU」への負荷はかなり小さいです。

---

## 2. Render Free プランの制限とこのプロジェクト

| 制限 | 内容 | このプロジェクトでの影響 |
|------|------|---------------------------|
| **スピンダウン** | 15 分間アクセスがないとスリープ、次のアクセスで最大約 1 分で起動 | 小規模・個人店なら「初回だけ遅い」で許容できることが多い。常時速い応答が必要なら Starter 検討。 |
| **750 時間/月** | Free インスタンスの合計稼働時間 | 1 サービスなら 24×31≒744 時間なので、スピンダウン中は消費しないため余裕で収まる。 |
| **エフェメラルディスク** | 再デプロイ・再起動でローカルファイルは消える | 商品データは `products.json`（リポジトリ）のみ。アップロードやログ保存をしていないので影響なし。 |
| **帯域（アウトバウンド）** | 無料枠を超えると課金 or サービス停止 | 画像・動画は R2 直読みのため、Render 経由の帯域は HTML/CSS/JS/API のみで少ない。 |
| **ビルド時間** | パイプライン分数の制限 | ビルドコマンドなしで `npm install` のみのため、消費は小さい。 |

---

## 3. 想定される利用パターン別

- **テスト・デモ・個人でゆるく運用**  
  → **Free のままで十分。**

- **「15 分後に誰かがアクセスすると最大 1 分待つ」を避けたい**  
  → **Starter（$7/月）** にすると常時起動になり、スピンダウンがなくなります。

- **アクセス数・売上がかなり増えてきた**  
  → その時点で Starter 以上や、帯域・同時接続の見直しを検討すればよいです。

---

## 4. 現在の render.yaml

```yaml
plan: free
```

このまま **Free** で問題ありません。  
有料にしたい場合は Render ダッシュボードで「Change Plan」から **Starter** を選べばよいです。

---

## 5. まとめ

| 質問 | 回答 |
|------|------|
| 一番低いプランでいける？ | **はい。Free プランで運用可能です。** |
| 今の構成のまま変える必要はある？ | **いいえ。R2 でメディアを配信している現状のままで大丈夫です。** |
| いつ有料を検討するか | スピンダウン（初回アクセス時の待ち時間）が気になったら **Starter $7/月** を検討。 |

以上を踏まえ、**現時点では Free のままで進めてよく、必要になったら Starter へ上げる**形で十分です。

---

## 6. 初回告知で 500 人一気に流入する場合

「初回告知で 500 人が一気に来る」想定での整理です。

### 大丈夫な部分

| 要素 | 理由 |
|------|------|
| **画像・動画** | すべて **Cloudflare R2** 直読み。Cloudflare は大トラフィック前提なので 500 人程度は問題なし。 |
| **Stripe 決済** | 決済ボタンを押した人だけ Stripe に飛ぶ。500 人が同時に「決済」することはほぼないので Stripe 側も余裕。 |
| **帯域** | 500 人 × 数百 KB（HTML/CSS/JS/API）でも、R2 にメディアを寄せているので Render 経由の帯域はそこまで大きくならない。 |

### リスクになる部分（Render Free）

1. **スピンダウン（冷たい起動）**  
   告知直前に 15 分以上誰もアクセスしていないと、**最初のリクエストでサービスが起動し始め、最大約 1 分かかる**ことがあります。  
   その間に 500 人が来ると、多くの人が「読み込み中」やタイムアウトを見る可能性があります。

2. **1 台の Free インスタンスに集中**  
   Free は **1 インスタンスのみ**。500 人が短時間に同時アクセスすると、  
   - 応答が遅くなる  
   - 一部でタイムアウトや 503 が出る  
   といった可能性はあります（公表されている同時接続の厳密な上限はないが、無料枠は余裕が少なめ）。

### 推奨対策（2 パターン）

**A. Free のまま運用する場合（コスト 0）**

- **告知の 5〜10 分前に、自分でサイトに 1 回アクセスする。**  
  → サービスを「起こしておく」ことで、告知直後のスピンダウンを避けられます。  
- それでも 500 人が **数十秒以内** に集中すると、遅延や一部エラーは起こり得ます。  
  「とにかく 0 円で乗り切りたい」場合はこの運用で様子見。

**B. 告知前だけ有料にして確実にしたい場合（推奨）**

- **告知する週（または数日）だけ Starter（$7/月）にアップグレード**する。  
  - スピンダウンなし（常時起動）  
  - 告知時刻にサービスが眠っていない  
  - インスタンスの余力も少し増える  
- 落ち着いたら Free に戻すことも可能（プラン変更はダッシュボードで随時可能）。

### まとめ（500 人流入）

| 選択肢 | リスク | コスト |
|--------|--------|--------|
| **Free のまま ＋ 告知前に 1 回アクセスで起こす** | スピンダウンは防げる。同時 500 人で遅延・一部エラーはあり得る。 | 0 円 |
| **告知期間だけ Starter に上げる** | 起動遅延なし。500 人程度の一時的な流入にかなり強い。 | 約 $7/月（使う期間だけ） |

**結論**: 「絶対に詰まらせたくない」なら **告知前の短期間だけ Starter** にするのが安心です。  
「まずは Free で試して、問題が出たらその時 Starter にすればいい」という割り切りでも運用は可能です。
