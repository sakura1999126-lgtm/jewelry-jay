# Stripeのテストモードと本番モードの設定

## 📌 Stripeには2つのモードがあります

### 1. テストモード（開発・テスト用）
- ✅ **実際のお金は動きません**
- ✅ **テストカードで決済できます**
- ✅ **無料で使えます**
- ✅ **テストキー**: `sk_test_...` で始まる

### 2. 本番モード（実際の決済用）
- ⚠️ **実際のお金が動きます**
- ⚠️ **本物のカードで決済します**
- ⚠️ **手数料がかかります**
- ⚠️ **本番キー**: `sk_live_...` で始まる

---

## 🎯 今すぐやること

### ステップ1: Stripeダッシュボードでモードを確認

1. **Stripeダッシュボード**にログイン: https://dashboard.stripe.com
2. 画面右上の**「テストモード」**または**「本番モード」**を確認
   - **「テストモード」**と表示されている → テストモード
   - **「本番モード」**と表示されている → 本番モード

### ステップ2: テストモードに切り替える（推奨）

**まずはテストモードで動作確認しましょう！**

1. Stripeダッシュボードの右上で**「テストモード」**をクリック
2. テストモードに切り替わる

### ステップ3: テストキーを取得

1. **「Developers」** → **「API keys」** をクリック
2. **「テストモード」**のセクションで：
   - **「Secret key」**の**「表示」**をクリック
   - `sk_test_...` で始まる文字列をコピー

### ステップ4: Renderで環境変数を更新

1. Renderダッシュボード → `jewelry-jay` サービス
2. **「Environment」** をクリック
3. **`STRIPE_SECRET_KEY`** の行の**「Edit」**をクリック
4. **Value**に、ステップ3でコピーしたテストキー（`sk_test_...`）を貼り付け
5. **「Save」**をクリック
6. 画面下部の**「Save Changes」**をクリック

### ステップ5: Webhookもテストモード用に設定

1. Stripeダッシュボードで**「テストモード」**になっていることを確認
2. **「Developers」** → **「Webhooks」** をクリック
3. エンドポイントURLが `https://west-tokyo-jewels.com/api/webhook` のエンドポイントを確認
   - もしなければ、新しく作成：
     - **「Add endpoint」**をクリック
     - **Endpoint URL**: `https://west-tokyo-jewels.com/api/webhook`
     - **イベント**: `checkout.session.completed` にチェック
     - **「Add endpoint」**をクリック
4. **「署名シークレット」**の**「表示」**をクリック
5. テストモード用の署名シークレットをコピー
6. Renderで `STRIPE_WEBHOOK_SECRET` を更新

---

## 🧪 テストカードの使い方

テストモードでは、以下のカード番号が使えます：

### 成功するカード
- **カード番号**: `4242 4242 4242 4242`
- **有効期限**: 未来の日付（例: 12/25）
- **CVC**: 任意の3桁（例: 123）

### その他のテストカード
- **3Dセキュア認証**: `4000 0027 6000 3184`
- **決済失敗**: `4000 0000 0000 0002`
- **残高不足**: `4000 0000 0000 9995`

---

## 🚀 本番モードに切り替えるタイミング

**テストモードで動作確認が完了してから、本番モードに切り替えます。**

### 本番モードに切り替える手順

1. **Stripeダッシュボード**で**「本番モード」**に切り替え
2. **「Developers」** → **「API keys」** で本番キー（`sk_live_...`）を取得
3. Renderで `STRIPE_SECRET_KEY` を本番キーに更新
4. Webhookも本番モード用に設定
   - 本番モードでWebhookエンドポイントを作成
   - 本番モード用の署名シークレットを取得
   - Renderで `STRIPE_WEBHOOK_SECRET` を更新

---

## 📋 チェックリスト

### テストモード設定
- [ ] Stripeダッシュボードで「テストモード」に切り替え
- [ ] テストキー（`sk_test_...`）を取得
- [ ] Renderで `STRIPE_SECRET_KEY` をテストキーに設定
- [ ] テストモード用のWebhookエンドポイントを作成
- [ ] テストモード用の署名シークレットを取得
- [ ] Renderで `STRIPE_WEBHOOK_SECRET` をテストモード用に設定
- [ ] テスト決済を実行して動作確認

### 本番モード設定（後で）
- [ ] テストモードで動作確認完了
- [ ] Stripeダッシュボードで「本番モード」に切り替え
- [ ] 本番キー（`sk_live_...`）を取得
- [ ] Renderで `STRIPE_SECRET_KEY` を本番キーに更新
- [ ] 本番モード用のWebhookエンドポイントを作成
- [ ] 本番モード用の署名シークレットを取得
- [ ] Renderで `STRIPE_WEBHOOK_SECRET` を本番モード用に更新

---

## ⚠️ 重要なポイント

- ✅ **まずはテストモードで動作確認**
- ✅ **テストモードでは実際のお金は動きません**
- ✅ **テストカードで何度でも試せます**
- ✅ **動作確認が完了してから本番モードに切り替える**

---

## 🎉 完了！

テストモードで設定すれば、安全に動作確認できます！
