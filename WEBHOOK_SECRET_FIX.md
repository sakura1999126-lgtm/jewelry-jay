# ⚠️ Webhook署名シークレットの修正が必要です

## 問題

現在、`STRIPE_WEBHOOK_SECRET` に **onrender.comの時の署名シークレット** が設定されていますが、現在のWebhookエンドポイントURLは **`https://west-tokyo-jewels.com/api/webhook`** です。

**署名シークレットは、エンドポイントURLごとに異なります。**

## 解決方法

### ステップ1: Stripeで正しい署名シークレットを確認

1. **Stripeダッシュボード**にログイン
2. **「Developers」** → **「Webhooks」** をクリック
3. **エンドポイントURLが `https://west-tokyo-jewels.com/api/webhook` のエンドポイント**をクリック
4. **「署名シークレット」**のセクションで **「表示」** をクリック
5. `whsec_...` で始まる文字列をコピー

### ステップ2: Renderで環境変数を更新

1. **Renderダッシュボード** → `jewelry-jay` サービス
2. **「Environment」** をクリック
3. **`STRIPE_WEBHOOK_SECRET`** の行の **「Edit」** をクリック
4. **Value** に、ステップ1でコピーした新しい署名シークレットを貼り付け
5. **「Save」** をクリック
6. 画面下部の **「Save Changes」** をクリック

### ステップ3: 動作確認

1. 再デプロイが完了するまで待つ（数分）
2. テスト決済を実行
3. Renderのログで以下が表示されれば成功：

```
✅ Webhook署名検証成功
```

もし以下のエラーが出る場合は、署名シークレットが間違っています：

```
❌ Webhook署名検証失敗
```

## 重要なポイント

- ✅ **エンドポイントURLごとに署名シークレットが異なります**
- ✅ **現在のエンドポイントURL用の署名シークレットを使う必要があります**
- ✅ **署名シークレットが間違っていると、Webhookが動作しません**
