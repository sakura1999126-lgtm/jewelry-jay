# Cloudflare Pages デプロイ設定

現在、プロジェクトが「Workers」として認識されているため、以下の設定が必要です。

## 推奨される解決方法

Cloudflare Pagesダッシュボードで以下を設定してください：

1. **「設定」タブ** → **「ビルドとデプロイ」**
2. **「ビルド」セクション**で「編集」をクリック
3. **「デプロイ コマンド」を以下のように変更**:
   ```
   npx wrangler deploy --assets=.
   ```
4. **「更新」をクリック**

これで静的サイトとしてデプロイされます。

---

## または、Pagesプロジェクトとして再作成（推奨）

1. 現在のプロジェクトを削除（オプション）
2. 左サイドバーの「Pages」をクリック（「Workers」ではない）
3. 「プロジェクトを作成」→「Gitに接続」
4. すべてのフィールドを空白のまま「保存してデプロイ」

Pagesプロジェクトとして作成すれば、デプロイコマンドは不要です。

