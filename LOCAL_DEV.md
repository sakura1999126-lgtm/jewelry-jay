# 修正をすぐ確認する（ローカルで見る）

デプロイせずに、**自分のPCでサイトを立ち上げてすぐ確認**する方法です。

## 手順

1. ターミナルでプロジェクトのフォルダに入る:
   ```bash
   cd /Users/sakurairintaro/jewelry-jay
   ```

2. サーバーを起動:
   ```bash
   npm run dev
   ```
   （または `npm start`）

3. ブラウザで開く:
   **http://localhost:3006**

4. HTML / CSS / script.js / products.json を編集して保存 → ブラウザで **更新（F5 または Cmd+R）** すれば変更がすぐ反映されます。

5. 終わったらターミナルで `Ctrl+C` でサーバーを止める。

---

**まとめ**: 修正のたびにデプロイしなくてOK。普段は `npm run dev` でローカル確認 → よくなったらコミット＆プッシュで本番に反映、という流れで大丈夫です。
