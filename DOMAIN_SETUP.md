# ドメイン取得後の設定手順（west-tokyo-jewels.com）

## ⚠️ 後悔しないための重要ポイント

- **ネームサーバー設定を忘れるとサイトにアクセスできない**
- **DNS反映には数時間〜24時間かかる**
- **設定ミスでサイトがダウンする可能性がある**

---

## ステップ1: Renderでカスタムドメインを追加

1. **Renderダッシュボード** → `jewelry-jay` サービスを開く
2. 左メニュー **「Settings」** → **「Custom Domains」**
3. **「Add Custom Domain」** をクリック
4. `west-tokyo-jewels.com` を入力
5. **「Add Domain」** ボタンをクリック
6. **⚠️ ここで表示されるネームサーバー情報をコピー**（例：`ns1.render.com`, `ns2.render.com`）
   - 画面にネームサーバー情報が表示されます
   - この情報をメモまたはコピーしてください

---

## ステップ2: お名前.comでDNSレコード設定

RenderがDNSレコード設定を要求している場合の手順：

1. **お名前.com** にログイン
2. **「ドメイン」** → **「DNS関連機能の設定」** → **「DNSレコード設定を利用する」**
3. `west-tokyo-jewels.com` を選択

### DNSレコードを追加：

**レコード1: wwwサブドメイン用（CNAME）**
- **ホスト名**: `www`
- **TYPE**: `CNAME`
- **値**: `jewelry-jay.onrender.com`
- **TTL**: 3600（デフォルト）

**レコード2: ルートドメイン用（Aレコード）**
- **ホスト名**: `@` または空白
- **TYPE**: `A`
- **値**: `216.24.57.1`
- **TTL**: 3600（デフォルト）

⚠️ **重要**: 
- ルートドメイン（@）にはCNAMEレコードは設定できません。必ずAレコードを使用してください
- 既存のレコードがある場合は削除してから追加してください

4. **「設定する」** をクリック

---

## 代替方法: ネームサーバー設定（推奨）

もしRenderがネームサーバー情報を表示している場合は、こちらの方が簡単です：

1. **お名前.com** にログイン
2. **「ドメイン」** → **「ネームサーバー設定」**
3. `west-tokyo-jewels.com` を選択
4. **「その他のネームサーバーを使う」** を選択
5. Renderからコピーしたネームサーバーを入力（例：`ns1.render.com`, `ns2.render.com`）
6. **「確認」** → **「設定する」**

---

## ステップ3: Renderでドメインを確認

1. Renderの「Custom Domains」画面に戻る
2. **「Verify」** ボタンをクリック
3. DNSレコードが正しく設定されていれば、ドメインが「Verified」状態になります
4. まだ反映されていない場合は、数時間待ってから再度「Verify」をクリック

---

## ステップ4: DNS反映を待つ（重要！）

- **反映時間**: 数時間〜24時間（最大72時間かかる場合もあり）
- **確認方法**: 
  - `nslookup www.west-tokyo-jewels.com` でCNAMEレコードを確認
  - `nslookup west-tokyo-jewels.com` でAレコードを確認
- **⚠️ 反映前に設定を変更すると不具合の原因になる**

---

## ステップ5: SSL証明書の自動発行

- Renderが自動でSSL証明書を発行（無料）
- 反映後、`https://west-tokyo-jewels.com` でアクセス可能になる
- 証明書発行まで数分〜数時間かかる場合あり

---

## ステップ6: 動作確認

1. `https://west-tokyo-jewels.com` にアクセス
2. サイトが表示されるか確認
3. SSL証明書が有効か確認（🔒マーク）

---

## トラブルシューティング

### サイトにアクセスできない
- DNSレコード設定が正しいか確認（wwwはCNAME、ルートドメインはAレコード）
- DNS反映を待つ（24時間以内、最大72時間）
- Renderの「Custom Domains」で「Verify」をクリックして確認
- お名前.comのDNSレコード設定画面で設定が正しく保存されているか確認

### SSL証明書が発行されない
- DNS反映を待つ
- Renderのログを確認
- 数時間待ってから再確認

### ネームサーバー情報が分からない
- Renderの「Custom Domains」画面で再確認
- Renderのサポートに問い合わせ

---

## 完了後の確認事項

- [ ] `https://west-tokyo-jewels.com` でアクセスできる
- [ ] SSL証明書が有効（🔒マーク）
- [ ] 商品ページが正常に表示される
- [ ] 決済機能が動作する

---

## 注意点

- **自動更新をオフにしたい場合**: お名前.comで自動更新を無効化（2年目以降の料金を避けるため）
- **サブドメイン（www）も使う場合**: Renderで `www.west-tokyo-jewels.com` も追加設定可能
- **メール設定**: 別途メールサーバー設定が必要（Renderはメール機能なし）
